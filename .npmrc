# Local-only project. Keep installs reproducible and quiet.
auto-install-peers=true
strict-peer-dependencies=false
# Native deps used here (libsql) ship prebuilt binaries; no global build tools needed.
