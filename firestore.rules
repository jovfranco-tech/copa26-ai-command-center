rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /poolPicks/{playerName} {
      allow read: if true;
      allow write: if playerName.size() > 0 && playerName.size() <= 30;
    }
  }
}
